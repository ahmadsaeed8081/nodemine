import React from "react";

function SectionHeading({ text }) {
  return (
    <h1 className="text-[#D1D5DB] font-Space-Mono font-bold text-xl	">{text}</h1>
  );
}

export default SectionHeading;
